// three_walls's LED-bar pixel map. The pass is shared verbatim with the other
// neon-quad instruments — see shaders_common/nano_led_bars.hlsl, which holds
// the cbuffer and the entry point as well as the painting, because every
// effect that publishes this map publishes exactly the same one.
#include "nano_led_bars.hlsl"
